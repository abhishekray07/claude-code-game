[project]
name = "claude-game-spike"
version = "0.1.0"
description = "Spike tests for Claude Code Learning Game"
requires-python = ">=3.11"
dependencies = [
    "modal>=0.66.0",
    "websockets>=12.0",
]

[tool.uv]
dev-dependencies = []
